# メンテナンス・チューニングガイド

このドキュメントでは、アドインの挙動を調整するためのチューニングパラメータや、メンテナンス時の注意点について記述します。

## ⚙️ チューニングパラメータ

### 空き時間検索における「長時間予定」の閾値

共通の空き時間検索機能には、「長時間（4時間以上）の予定を、調整可能な予定として除外する」オプションがあります。この「4時間」という判定値はコード内で管理されています。

#### 変更が必要なファイル

値を変更する場合は、ロジックとUIの両方を更新してください。

1.  **ロジック**: [src/utils/scheduler.ts](file:///c:/Dev/outlook-calendar-addon/OutlookCalendarManager/src/utils/scheduler.ts#L48)

    ```typescript
    // 例: 判定を 2時間(120分) に変更する場合
    if (durationMinutes >= 120) return false;
    ```

2.  **UI表示（ラベル）**: [src/taskpane/components/CommonFreeTimeModal.tsx](file:///c:/Dev/outlook-calendar-addon/OutlookCalendarManager/src/taskpane/components/CommonFreeTimeModal.tsx#L208)
    ```tsx
    // 手動でラベルのテキストを更新してください
    <label title="2時間以上の予定は...">2時間以上の予定を除く</label>
    ```

## 🛠️ その他のメンテナンス

- **メンバー・グループ構成**: `src/config/groupConfig.ts` にて管理しています。
- **Azure App Registration**: `README.md` の「Azure App Registration」セクションを参照してください。
